name: Release

on:
  release:
    types: [published]

env:
  YARN_CHECKSUM_BEHAVIOR: update

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.release.prerelease && 'beta' || 'main' }}

      - name: Ensure beta branch checkout
        if: ${{ github.event.release.prerelease == true }}
        run: |
          git fetch origin beta --depth=1
          git checkout beta
          git reset --hard origin/beta

      - name: Ensure main branch checkout
        if: ${{ github.event.release.prerelease != true }}
        run: |
          git fetch origin main --depth=1
          git checkout main
          git reset --hard origin/main

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'
          cache: 'yarn'

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build
        run: yarn build

      - name: Publish with semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          YARN_NPM_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          branch=$([ "${{ github.event.release.prerelease }}" = "true" ] && echo 'beta' || echo 'main')
          corepack yarn semantic-release --no-ci --branches "$branch"
