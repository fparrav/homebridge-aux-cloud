{
  "pluginAlias": "AuxCloudPlatform",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "title": "AUX Cloud Platform",
    "type": "object",
    "required": [
      "name",
      "username",
      "password"
    ],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Aux Cloud"
      },
      "username": {
        "title": "AUX Cloud Username",
        "type": "string",
        "description": "Email address or phone number registered with AUX Cloud."
      },
      "password": {
        "title": "AUX Cloud Password",
        "type": "string"
      },
      "region": {
        "title": "Region",
        "type": "string",
        "default": "eu",
        "enum": [
          "eu",
          "usa",
          "cn"
        ],
        "description": "Select the AUX Cloud region that matches your account."
      },
      "baseUrl": {
        "title": "Custom API Base URL",
        "type": "string",
        "placeholder": "https://app-service-xxx.smarthomecs.com",
        "description": "Optional override for AUX Cloud API host. Provide this if your AC Freedom app reports \"Other Areas\" or a custom server."
      },
      "temperatureUnit": {
        "title": "Temperature Unit",
        "type": "string",
        "default": "C",
        "enum": [
          "C",
          "F"
        ],
        "description": "Select the unit used when displaying temperatures. Values are converted automatically when talking to AUX Cloud."
      },
      "temperatureStep": {
        "title": "Target Temperature Step",
        "type": "number",
        "default": 0.5,
        "enum": [
          0.5,
          1
        ],
        "description": "Choose the increment for setpoints. 0.5 replicates AC Freedom, 1 limits adjustments to whole degrees. Fahrenheit mode enforces 1°F."
      },
      "pollInterval": {
        "title": "Polling Interval (seconds)",
        "description": "How frequently the plugin should request updated state from AUX Cloud.",
        "type": "integer",
        "default": 60,
        "minimum": 30,
        "maximum": 600
      },
      "includeDeviceIds": {
        "title": "Included Device IDs",
        "type": "array",
        "description": "Optional list of AUX endpoint IDs to expose. Leave empty to include every discovered device.",
        "items": {
          "title": "Endpoint ID",
          "type": "string"
        }
      },
      "excludeDeviceIds": {
        "title": "Excluded Device IDs",
        "type": "array",
        "description": "Optional list of AUX endpoint IDs to skip after discovery.",
        "items": {
          "title": "Endpoint ID",
          "type": "string"
        }
      }
    }
  }
}
